# Suggested controlled task

Add a `cursor_page` helper to `src/audit_api/pagination.py` and tests that prove stable
forward pagination. Preserve the existing `page` function and public import behavior.
Do not add runtime dependencies or change repository tooling.
